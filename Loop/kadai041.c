#include <stdio.h>
main()
{
	int num,sum, i;
	sum = 0, i = 0,num=0;
	while (num != -999)
	{
		sum += num;
		i++;
		printf("®”(-999‚ÅI—¹)?");
		scanf("%d", &num);
	}
	printf("‡Œv=%d\n",sum);
	printf("•½‹Ï=%.2f", (float)sum / (i-1));
}