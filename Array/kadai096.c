#include <stdio.h>
main()
{
	int num,i,c[10],j;
	for (i = 0; i < 10; i++)
	{
		printf("®”(-999‚ÅI—¹)?");
		scanf("%d", &num);
		if (num == -999)
		{
			break;
		}
		c[i] = num;
	}
		printf("”z—ñc=");
		for (j = 0; j < i; j++)
		{
			printf("%d ", c[j]);
		}
}